# NavHub 环境变量配置示例
# 复制此文件为 .env.local 并填入实际值

# OAuth 配置
# 是否启用 OAuth 登录（true/false）
VITE_OAUTH_ENABLED=false

# GitHub OAuth App Client ID
VITE_OAUTH_CLIENT_ID=

# Cloudflare Worker URL（用于 token 交换）
VITE_OAUTH_WORKER_URL=

# OAuth 回调地址（必须与 GitHub OAuth App 中的设置一致）
VITE_OAUTH_REDIRECT_URI=
